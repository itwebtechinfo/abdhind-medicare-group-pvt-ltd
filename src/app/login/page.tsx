import { LoginForm } from "@/src/components/auth/LoginForm";

export const metadata = {
  title: "Staff Login | Abd Hind MediCare ERP",
  description: "Secure hospital management portal login",
};

export default function LoginPage() {
  return <LoginForm />;
}
