/** Shared by public-booking and patient self-signup — kept here (not in either feature file)
 * to avoid a circular import between the two. */
export const OTP_LENGTH = 6;
export const OTP_RESEND_COOLDOWN_SECONDS = 60;
